# flutter_jissen_1 を最新Flutter + Swift Package Manager構成で立ち上げる

## Context（背景）

`flutter_jissen_1/` は現在 `readme.md`（空ファイル）が1つあるだけの空ディレクトリで、まだFlutterプロジェクトになっていない。ユーザーはここに新しい学習用Flutterプロジェクトを作りたいが、以下の希望がある。

- iOS/macOSのネイティブ依存管理は **CocoaPodsではなくSwift Package Manager(SPM)** ベースにしたい
- **FlutterとfvmはどちらもPCにインストール済みの中で最新版**を使いたい
- 学習目的なので、**実際のソースコードは書かず**、コマンド実行とその意味の平易な日本語説明を中心に進めてほしい

調査の結果：
- リポジトリ直下（`/Users/systemi/dev/flutter-plactice`）には `.fvmrc` があり、Flutter `3.35.7` に固定されている。同ディレクトリ配下の既存プロジェクト `widget_practice`（CocoaPodsベース）はこの固定バージョンを使っている。
- ユーザーへの確認の結果、**今回の「最新化」は新規プロジェクト `flutter_jissen_1` に限定し、リポジトリ直下の `.fvmrc`（3.35.7固定）や既存の `widget_practice` には手を入れない**方針で進める。
- fvm CLI自体は Homebrew管理で `4.0.1` が入っているが、最新は `4.1.2`（2026-06-25リリース）。fvm CLI自体はプロジェクト非依存の共有ツールなので、これはアップグレードして問題ない。
- Flutterの最新安定版は `3.44.x`（2026-05リリース、Dart 3.12）。**Flutter 3.44からSwift Package Managerがデフォルトで有効になり、`flutter create`しても`Podfile`が生成されなくなった**。つまり最新Flutterでプロジェクトを作るだけで、追加設定なしにSPMベースになる。
- Xcode 16.4 / CocoaPods 1.16.2 がインストール済み。SPM対応の前提（Xcode 15以上）は満たしている。

## 実施内容

### 1. fvm CLI自体を最新化
- `brew upgrade fvm` を実行し、`4.0.1` → 最新版（`4.1.2`系）にアップグレードする。
- これはPC全体で共有されるツールのアップデートであり、既存プロジェクトの固定Flutterバージョン（3.35.7）には影響しないことを説明する。

### 2. 最新のFlutter安定版をfvmでインストール
- `fvm install stable` を実行し、最新の安定版Flutter（実行時点の最新版。調査時点では`3.44.7`前後）をローカルにキャッシュする。
- バージョン一覧は `fvm releases` や `fvm list` で確認し、実際にインストールされたバージョン番号をユーザーに明示する。

### 3. `flutter_jissen_1` だけをそのバージョンにスコープする
- `cd flutter_jissen_1 && fvm use <取得した最新バージョン>` を実行する。
- これにより `flutter_jissen_1/.fvmrc` と `flutter_jissen_1/.fvm/` が作られ、**このディレクトリ配下だけ**リポジトリ直下の3.35.7固定を上書きする（fvmはディレクトリ階層を辿って一番近い`.fvmrc`を優先するため）。
- 既存の `widget_practice` や親ディレクトリの `.fvmrc` は変更しない。

### 4. プロジェクトの雛形を生成（ソースコードは書かない）
- 既に空の `flutter_jissen_1` ディレクトリ内で `fvm flutter create .` を実行し、Flutter標準の雛形一式（`lib/main.dart`、`ios/`、`android/`、`macos/`など）を生成する。
- これはFlutter CLIが自動生成するものであり、こちらで手書きのDart/Swiftコードは書かない。プロジェクト名はディレクトリ名 `flutter_jissen_1` から自動決定される点を説明する。

### 5. Swift Package Manager構成の確認
- `fvm flutter config` を実行し、`enable-swift-package-manager` が有効（Flutter 3.44+ではデフォルトtrue）になっていることを確認する。
- 生成された `ios/` と `macos/` ディレクトリを確認し、**`Podfile`が存在しないこと**、代わりにSPM関連の設定（`Flutter/Package.swift`ではなくXcodeプロジェクト内のSPM参照）が使われていることを説明する。
- CocoaPods自体はアンインストールしない（Flutterが将来的にSPM未対応のプラグイン向けにフォールバックとして使う可能性があるため）。

### 6. 動作確認（検証）
- `fvm flutter doctor` で環境に問題がないか確認。
- `fvm flutter pub get` → `fvm flutter analyze` で雛形が正しく解析できることを確認。
- 可能であれば `fvm flutter run -d macos`（またはChromeなど利用可能なデバイス）でデフォルトのカウンターアプリが実際に起動することを確認し、SPM構成のままビルドが通ることを見せる。
- 各コマンドの実行結果を、学習者向けに「これは何をしているコマンドか」「CocoaPodsとの違いは何か」を平易な日本語で説明しながら進める。

## 進め方の方針
- 各ステップはコマンド実行→結果表示→日本語での意味説明、という順番で進め、ソースコードそのものは書かずに`flutter create`が生成したファイルをそのまま使う。
- 既存の `widget_practice` プロジェクトやリポジトリ直下の `.fvmrc` には一切変更を加えない。
